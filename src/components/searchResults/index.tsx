import React, { FunctionComponent } from "react";

interface Props {
    searchQuery: string
}

const SearchResults: FunctionComponent<Props>  = ({searchQuery}: Props) => {
    return (
        <div></div>
    )
}

export default SearchResults;